#include <stdlib.h>
#include <stdio.h>

int main()
{
	char arr[50];
	int k = 0;
	int i = 0;
	printf("enter sentense - ");
	gets_s(arr);
	while (*(arr + i) != '\0')
	{
		if (arr[i] >= 'A' && arr[i] <= 'Z') {
			k++;
		}
		i++;
	}
	printf("number of characters - %d", k);

    return 0;
}
